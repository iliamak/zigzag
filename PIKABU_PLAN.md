# «Зигзаг» — Pikabu-версия — зафиксированные решения

Дата: 2026-09-20. Статус: согласовано с Ильёй. Читать перед работой над Pikabu-версией.
Техдока SDK: `PIKABU_SDK.md`. Дока прототипа: `PROJECT_DOCUMENTATION.md`.

## 1. Продукт
«Зигзаг» — отдельная Pikabu-версия игры (не ежедневка): уровневая головоломка в духе Zip/Numberlink.
Убрать из прототипа: VK Bridge, ежедневный seed, стрики/серии, напоминания, друзья-коды,
сторонние шаринги (VK/TG), лок «одна игра в день». Оставить поддержку-ссылку (разрешена).

## 2. Уровни: 100 штук, запечены в билд
Генерация — офлайн один раз скриптом `gen-levels.mjs`, результат — JSON в билде.
При сборке каждый уровень проходит selftest-гейт: решается, решение единственное.
У каждого уровня поле `hint` — первая подсказка с пустого поля (запечена офлайн,
мгновенная, без перебора в браузере).

## 3. Лесенка размеров (утверждена)
- Уровни 1–10: 4×4 (обучение, победы за секунды)
- Уровни 11–35: 5×5 (втягивание)
- Уровни 36–70: 6×6 (ядро, как сейчас)
- Уровни 71–100: 7×7 (эксперт)
- ОТКРЫТЫЙ ВОПРОС: мелкие клетки 7×7 на телефоне. Если плохо — кап на 6×6,
  сложность конца добираем флажками и стенками.

## 4. Флажки масштабируются с полем (~четверть-треть клеток)
- 4×4 → 4 флажка; 5×5 → 5–6; 6×6 → 7–8; 7×7 → 8–9.
- Шаг флажков вдоль решения — регулятор сложности: густо = легко, редко = сложно.

## 5. Генератор: фабрика + контролёр (generate-and-test)
1. Змея: гамильтониан через все клетки (змейка + backbite-перемешивание, детерминированный seed).
2. Флажки 1–N на позициях решения.
3. Стенки 0–6, только НЕ на рёбрах решения (решение всегда проходимо).
4. Решатель (DFS с прунингом, стоп на 2-м решении):
   - решений ровно 1, иначе брак (нечестный уровень);
   - метрики: средний бранчинг, доля вынужденных ходов, число поворотов,
     длина самого свободного сегмента между флажками.
5. Раскладка по ведрам сложности внутри каждого размера → отбор 100.
6. Запекание в JSON + selftest-гейт.
7. Калибровка по живым данным (время, подсказки) — двигать границы ведер.

## 6. Бэкенд: Bothost Basic 99 ₽/мес (сна и стирания нет, вопрос закрыт)
Один Python-процесс: раздача `pikabu.html` как статики + `POST /api/save` +
`GET /api/load?player_id=` + CORS для iframe Пикабу.
Клиент `pikabuStorage`: primary — API, fallback — localStorage;
при `userAuthorized` — перечитать `sdk.player.id`, смигрировать прогресс.

## 7. База: Supabase (создать новый проект, аккаунт Ильи)
Таблица `saves (player_id text PK, progress jsonb, updated_at timestamptz)`.
Service_role ключ — только в env Bothost, никогда в клиент.
JWT-проверка подписи (секрет Студии) — TECH DEBT, отложить; зафиксировать здесь при внедрении.

## 8. TG-бот (доступ только у Ильи)
Без изменений + добавить: уведомления админу об ошибках сейвов, команда `/stats`
(число игроков/сейвов). Отдельной админки нет.

## 9. Монетизация (SDK) — ФИНАЛ, 2026-09-20
1. Preloader при загрузке (только мобайл, до `gameStarted()`, параллельно с загрузкой уровней).
2. Fullscreen между уровнями — раз в 4–5 пройденных (счётчик, не подряд; на 6×6–7×7
   интервал сам даёт >120с кулдауна). Магазина нет.
3. Rewarded за подсказку — только по клику, награда при `reward === true`.
4. Кнопка «без рекламы» (перечёркнутый AD, без витрины): прямой
   `purchase({productId: 'no_ads'})`, non_consumable; после покупки оба формата гаснут,
   подсказки бесплатные. При старте — `getPurchases()` (восстановить «уже куплено»).
   Места кнопки: пауза + экран победы (+ позже решить про экран уровней).
   Гость → сначала `openAuthDialog()`, потом оплата.
Интеграция: `PkbSDK.init()` один раз → `gameStarted()` → player/ads/store.
Игра обязана работать с адблоком (всё через `canShow()`).
Гипотеза дохода: ставки за показ Пикабу не публикует; ориентир рынка ~20–30 ₽ eCPM/1000,
доля разработчика ~50% → ~10–15 ₽/1000 показов; 5000 ₽/мес ≈ 350–500 тыс. показов.
Первый месяц — замер по статистике Студии.

## 10. Пайплайн запуска
Генератор → запекание 100 → вёрстка под 4 размера (CSS-сетка и цифры от SIZE) →
SDK + сейвы → Supabase + деплой Bothost → Студия (иконки 1024 + 1920×1080,
описания, жанр) → тестовая ссылка → модерация (до 3 раб. дней).
Блокеры модерации: реквизиты (ИП/самозанятость/ООО РФ + счёт РФ) заранее;
облачные сохранения — наш API; ≥30 мин суммарного геймплея; русский интерфейс.

## 11. Солюшны: dev vs build (правило сборки)
- В `levels.json` у каждого уровня есть поле `_solution` — правильный путь.
  Оно нужно только для тестов (selftest-гейт: уровень решается, решение единственное).
- Правило: в dev-разработке `_solution` ЕСТЬ, в билде игры его НЕТ.
- При сборке билда для Пикабу скрипт `build-levels.mjs` обязан вычищать `_solution`
  из JSON (иначе спойлер: ответ подсмотрят в коде страницы).
- Перед тестом игры солюшны восстанавливаются: тест всегда гоняется на полной
  версии с `_solution`, билд — на вычищенной.
- Проверка сборки: в вычищенном JSON не должно остаться ни одного ключа `_solution`
  (grep-гейт в скрипте).

## 12. Статус на 2026-09-20 (зафиксировано)
Готово и проверено в headless-браузере (Playwright, ошибок ноль):
- `zigzag.html` — игра: доска 4–7, 100 уровней, экран уровней, прогресс
  (localStorage), SDK-хуки (preloader/fullscreen раз в 4–5/rewarded/без рекламы),
  selftest 19/19 (`?selftest=1`).
- `gen-levels.mjs` → `levels.json` (100, решения единственные) →
  `build-levels.mjs` → `levels.build.json` (без `_solution`, + поле `hint`).
- Локальный тест: `node serve.cjs 8000` → http://localhost:8000/zigzag.html
  (через file:// не работает — нужен сервер из-за fetch).
- GitHub: https://github.com/iliamak/zigzag (автопуш → автодеплой подключён).
- Прод: https://zigzag-kappa.vercel.app/ (корень — зигзаг; ежедневка «шаг»
  в архиве на /shag.html; /zigzag.html редиректит на корень).
- `index.html` (ежедневка «шаг») не тронут.
Следующее: бэкенд Bothost + Supabase (п.6–7), иконки/тексты для Студии,
тестовая ссылка, модерация. Открытые вопросы: мелкие клетки 7×7 на телефоне;
место кнопки «без рекламы» на экране уровней.

## 13. Идеи (бэклог, не в скоупе запуска)
- 2026-09-21 — Дейли-режим как отдельный LiveOps-режим (и для Пикабу, и для портирования на Яндекс):
  концепт зафиксирован, реализация после запуска 100 уровней.
  - Отдельный пункт меню, не ломает прогресс 100 уровней.
  - 1 задача в день по дате (переиспользовать seed-генератор из «шаг»): реиграбельность + возврат.
  - Каждый день — свой визуал/тема («сегодня день такой-то»): цвета, фон, оформление флажков/стенок.
    Контентно дёшево: механика та же, меняется только скин + seed.
  - Для Яндекса закрывает п.2.9 (мотивация перепроходить) и удержание; для Пикабу — дешёвый LiveOps без новых уровней.
  - Открыто: разрешить ли перепрохождение дейли в тот же день; стрик/календарь; награда за серию дейли.
