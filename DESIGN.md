# DESIGN.md — дизайн-система «шага» / «зигзага»

Выжимка визуального языка проекта для переиспользования. Источник: `index.html` (инлайн-CSS).
Дата: 2026-09-20.

## 1. Характер
Тёплая «газетная» спокойность + один энергичный акцент. Не дашборд: одна композиция,
воздух, крупные заголовки. Правило: карточки только у интерактива (диалоги, правила,
элементы управления) — никаких декоративных обёрток вокруг показателей.

## 2. Токены
```css
--paper: #f7f7f2;        /* фон страницы */
--white: #fff;           /* подложки */
--ink: #292a27;          /* текст, тёмные плашки */
--muted: #8b8c84;        /* вторичный текст */
--line: #e3e4db;         /* разделители, сетка */
--orange: #f5753c;       /* бренд, путь, акценты */
--orange-dark: #d95822;  /* текст-акцент, hover */
--orange-soft: #fff0e6;  /* мягкие подложки */
--green: #57745a;        /* успех */
--font: 'Golos Text', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

## 3. Типографика
- Golos Text 400–900, фолбэк на системный. Кириллица из коробки.
- Фирменный приём: огромный рыхлый заголовок (80px/800/letter-spacing −6px)
  + цветная точка `brand-period` в конце слова (`шаг.`).
- Мелкий текст 10–13px, межстрочный 1.6–1.9, приглушённый цвет — «тихие» подписи.
- Табличные цифры: `font-variant-numeric: tabular-nums` для таймеров/счётчиков.

## 4. Форма и радиусы
Радиус растёт с размером элемента: кнопки 8–9px → поле/карточки 12–13px → диалоги 20px.
Кнопки: `button-primary` (ink), `button-orange`, `button-secondary` (белая с рамкой),
`button-subtle`, `button-full`. Hover: подъём на 1px + затемнение. Disabled: opacity .35.
Фокус: `outline: 2px solid var(--orange)`, offset 5px.

## 5. Игровое поле (паттерн)
- Белая подложка, рамка 2px ink, скругление 12px, `aspect-ratio: 1`, touch-action none.
- Сетка SVG-линиями `#e2e4da`, путь — градиент `#ffb16c → #f5753c`, linecap/linejoin round,
  ширина ~7% клетки. Стены — `#363831`, ширина 6.5, round caps.
- Цифры: тёмный круг 33px; стартовая — оранжевая + пульсирующее кольцо (`start-pulse`,
  гаснет после первого хода). Посещённые — подсветка `#ffdfc6`.
- Пауза: блюр-оверлей `rgba(247,247,242,.92) + blur(7px)`, круглый символ, заголовок + одна кнопка.

## 6. Анимации (всё — чистый CSS, без библиотек)
| Эффект | Реализация |
|---|---|
| Каскадный вход блоков | `enter-up`/`board-enter`, задержки 0 → 0.22s |
| Живая демо-линия в правилах | `route-demo` 7s infinite (stroke-dashoffset) |
| Пульс старта | `start-pulse` 2.8s infinite |
| Ход/ошибка/подсказка/победа | `number-pop` .35s / `invalid-cell` .4s / `hint-pulse` 1.6s / `complete-glow` 1.3s |
| Диалог/тост | `dialog-enter` .25s (снизу-вверх) / слайд тоста |
| Конфетти победы | JS-дивики, падение 2.5s, `prefers-reduced-motion` — скрыть |
- Глобально: `prefers-reduced-motion: reduce` глушит все анимации и показывает
  конечные состояния (линии дорисованы). Копировать блок целиком.

## 7. Иконки
Скрытый SVG-спрайт `<symbol id="i-...">`, подключение `<use href="#i-bell">`.
Класс `.icon`: 20px, stroke 1.7, round caps. Набор: users, bell, chevron, arrow, clock,
pause, play, undo, reset, bulb, hand, check, close, share, copy, download, calendar,
info, spark, telegram, trophy. Хелпер `icon(name)` для JS-рендера модалок.

## 8. Диалоги (один `<dialog>` на всё)
`openDialog(kind, content, centered)`: сохраняет фокус, лочит скролл body,
фокус в заголовок с `id="modalTitle"`, возврат фокуса при закрытии.
Виды: result, friends, share, hint, reset, reminder, tutorial, about, invitation, copy.
Стиль: paper-фон, рамка `rgba(55,63,39,.09)`, тень `0 25px 100px rgba(36,41,29,.13)`,
бэкдроп `rgba(39,43,31,.28) + blur(4px)`. Eyebrow 10px/letterspacing 1.6px/muted,
заголовок 28px/600/−1.1px.

## 9. Адаптив (брейкпоинты)
1400 / 1240 / 1000 / 860 / 580 / 365. Приёмы: ≥861px — две колонки
(игра 556px + сайд 326px); 580–860 — сайд в 2 колонки, затем в ленту;
поле всегда `width:100% + aspect-ratio:1`; цифры 33→29→25px; мелочь 320px —
прятать иконки в навигации. Режим `body.vk-mini-app` прячет верхнюю навигацию
(правый угол занят нативной панелью).

## 10. Доступность (копировать по умолчанию)
`lang="ru"`, роль grid + roving tabindex на клетках, `role="status"` на сообщениях,
видимые фокусы, кнопки вместо дивов, текстовые aria-label, SR-only хелп,
поддержка полного прохождения с клавиатуры (стрелки/Enter/Backspace/P).

## 11. Что едет в Pikabu-версию, что остаётся
- Едет: токены, типографика, кнопки, поле, анимации, иконки, диалог, адаптив, a11y.
- Остаётся в ежедневке: стрик-блок «День за днём», напоминания, друзья/шаринг,
  daily-метка. Заменяется: экран выбора 100 уровней, счётчик fullscreen (4–5),
  rewarded-подсказка, кнопка «без рекламы».
